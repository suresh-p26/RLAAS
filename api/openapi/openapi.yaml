openapi: 3.0.3
info:
  title: RLAAS API
  version: 0.1.0
paths:
  /rlaas/v1/check:
    post:
      summary: Evaluate a rate limit decision
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Decision response
